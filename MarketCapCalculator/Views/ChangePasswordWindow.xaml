<Window x:Class="MarketCapCalculator.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cambio Password"
        Height="500"
        Width="420"
        Background="#121212"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Segoe UI"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow"
        Closing="Window_Closing">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- <TextBlock Grid.Row="0" Text="" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/> -->
        
        <TextBlock Grid.Row="1" Text="CAMBIO PASSWORD" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="2" Text="Vecchia password:" FontSize="11" Foreground="#CCCCCC" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="3" x:Name="OldPasswordInput" Height="36" FontSize="13" Foreground="#FFFFFF" Background="#2D2D2D" BorderBrush="#4A4A4A" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Nuova password:" FontSize="11" Foreground="#CCCCCC" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="5" x:Name="NewPasswordInput" Height="36" FontSize="13" Foreground="#FFFFFF" Background="#2D2D2D" BorderBrush="#4A4A4A" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="6" Text="Ripeti nuova password:" FontSize="11" Foreground="#CCCCCC" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="7" x:Name="ConfirmNewPasswordInput" Height="36" FontSize="13" Foreground="#FFFFFF" Background="#2D2D2D" BorderBrush="#4A4A4A" VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="8" x:Name="ErrorText" Text="" FontSize="11" Foreground="#FF5252" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,10,0,10"/>

        <Grid Grid.Row="9" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="CONFERMA" Click="ConfirmButton_Click" Background="#00C853" Foreground="#1A1A1A" FontWeight="Bold" Height="40" Cursor="Hand"/>
            <Button Grid.Column="2" Content="ANNULLA" Click="CancelButton_Click" Background="#444444" Foreground="#FFFFFF" Height="40" Cursor="Hand"/>
        </Grid>

        <Button Grid.Row="10" Content="Recupera via Telegram" Click="ForgotPassword_Click" Background="Transparent" Foreground="#4A90E2" FontSize="11" Cursor="Hand" BorderThickness="0" HorizontalAlignment="Center" Margin="0,15,0,0"/>
    </Grid>
</Window>